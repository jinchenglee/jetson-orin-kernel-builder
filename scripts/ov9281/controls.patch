--- a/nvidia-oot/drivers/media/i2c/nv_ov9281.c
+++ b/nvidia-oot/drivers/media/i2c/nv_ov9281.c
@@ -43,7 +43,11 @@
 #include "../platform/tegra/camera/camera_gpio.h"
 #include "ov9281_mode_tbls.h"
 
-#define OV9281_SENSOR_INTERNAL_CLK_FREQ   800000000
+/* From the local ov9282 reference: VTS 1742 and a 12-line margin. */
+#define OV9281_DEFAULT_FRAME_LENGTH 1742U
+#define OV9281_MAX_FRAME_LENGTH 65535U
+#define OV9281_EXPOSURE_MARGIN 12U
+#define OV9281_GROUP_HOLD 0x3308
 
 static const struct of_device_id ov9281_of_match[] = {
 	{.compatible = "ovti,ov9281",},
@@ -55,6 +59,7 @@
 static const u32 ctrl_cid_list[] = {
 	TEGRA_CAMERA_CID_GAIN,
 	TEGRA_CAMERA_CID_EXPOSURE,
+	TEGRA_CAMERA_CID_FRAME_RATE,
 	TEGRA_CAMERA_CID_SENSOR_MODE_ID,
 };
 
@@ -85,6 +90,8 @@
 	struct tegracam_device *tc_dev;
 	struct i2c_client *rom;
 	struct inno_rom_table rom_table;
+	u32 frame_length;
+	s64 exposure_us;
 };
 
 static const struct regmap_config sensor_regmap_config = {
@@ -214,7 +221,7 @@
 
 	//if (val == 0)
 	//	return -EINVAL;
-	gain = val;
+	gain = val; /* gain_factor=16: control 16 means 1x, register 0x10. */
 	    //(s16) (OV9281_ANALOG_GAIN_C0 -
 	//	   (mode->control_properties.gain_factor *
 	//	    OV9281_ANALOG_GAIN_C0 / val));
@@ -241,87 +248,78 @@
 	return err;
 }
 
+/* Controls use the units advertised by mode0 (microseconds and micro-fps). */
 static int ov9281_set_exposure(struct tegracam_device *tc_dev, s64 val)
 {
 	struct camera_common_data *s_data = tc_dev->s_data;
-	//struct ov9281 *priv = (struct ov9281 *)tc_dev->priv;
-	struct device *dev = tc_dev->dev;
+	struct ov9281 *priv = tegracam_get_privdata(tc_dev);
 	const struct sensor_mode_properties *mode =
-	    &s_data->sensor_props.sensor_modes[s_data->mode_prop_idx];
-
-	int err = 0;
-	ov9281_reg ct_regs[3];
-
-	const s32 max_coarse_time = OV9281_MAX_COARSE_EXPOSURE;
-	s32 fine_integ_time_factor;
-	u32 coarse_time;
-	int i;
-
-	if (mode->signal_properties.pixel_clock.val == 0 ||
-		 mode->control_properties.exposure_factor == 0 ||
-		 mode->image_properties.line_length == 0)
+		&s_data->sensor_props.sensor_modes[s_data->mode_prop_idx];
+	ov9281_reg regs[3];
+	u64 lines;
+	u32 frame_length = priv->frame_length ?: OV9281_DEFAULT_FRAME_LENGTH;
+	int i, err;
+
+	if (val <= 0 || !mode->signal_properties.pixel_clock.val ||
+	    !mode->control_properties.exposure_factor ||
+	    !mode->image_properties.line_length)
 		return -EINVAL;
-
-	fine_integ_time_factor = mode->control_properties.exposure_factor ;
-
-	dev_dbg(dev, "%s: Setting exposure control to: %lld factor %d\n", __func__, val, fine_integ_time_factor);
-
-	coarse_time = val;
-	 //(val - fine_integ_time_factor)
-	  //   * OV9281_SENSOR_INTERNAL_CLK_FREQ
-	   //  / mode->control_properties.exposure_factor
-	    // / mode->image_properties.line_length;
-
-	if (coarse_time < OV9281_MIN_COARSE_EXPOSURE)
-		coarse_time = OV9281_MIN_COARSE_EXPOSURE;
-	else if (coarse_time > max_coarse_time) {
-		coarse_time = max_coarse_time;
-		dev_dbg(dev,
-			"%s: exposure limited by frame_length: %d [lines]\n",
-			__func__, max_coarse_time);
-	}
-
-	dev_dbg(dev, "%s: val: %lld [us], coarse_time: %d [lines]\n",
-		__func__, val, coarse_time);
-
-	ov9281_get_coarse_integ_time_regs(ct_regs, coarse_time);
-
-	for (i = 0; i < 2; i++) {
-		err = ov9281_write_reg(s_data, ct_regs[i].addr, ct_regs[i].val);
-		if (err) {
-			dev_dbg(dev,
-				"%s: coarse_time control error\n", __func__);
+	lines = (u64)val * mode->signal_properties.pixel_clock.val /
+		mode->control_properties.exposure_factor /
+		mode->image_properties.line_length;
+	lines = clamp_t(u64, lines, 1, frame_length - OV9281_EXPOSURE_MARGIN);
+	/* OV9281 exposure is a 20-bit value in sixteenths of a line. */
+	ov9281_get_coarse_integ_time_regs(regs, (u32)lines << 4);
+	for (i = 0; i < ARRAY_SIZE(regs); i++) {
+		err = ov9281_write_reg(s_data, regs[i].addr, regs[i].val);
+		if (err)
 			return err;
-		}
-	}
-
-	return err;
-}
-
-static int imx477_set_group_hold(struct tegracam_device *tc_dev, bool val)
-{
-        //struct camera_common_data *s_data = tc_dev->s_data;
-        struct device *dev = tc_dev->dev;
-        //int err;
-
-        dev_dbg(dev, "%s: Setting group hold control to: %u\n", __func__, val);
-
-        //err = imx477_write_reg(s_data, IMX477_GROUP_HOLD_ADDR, val);
-        //if (err) {
-        //        dev_err(dev, "%s: Group hold control error\n", __func__);
-        //        return err;
-        // }
-
-        return 0;
-}
-
+	}
+	priv->exposure_us = val;
+	return 0;
+}
+
+static int ov9281_set_frame_rate(struct tegracam_device *tc_dev, s64 val)
+{
+	struct camera_common_data *s_data = tc_dev->s_data;
+	struct ov9281 *priv = tegracam_get_privdata(tc_dev);
+	const struct sensor_mode_properties *mode =
+		&s_data->sensor_props.sensor_modes[s_data->mode_prop_idx];
+	u64 frame_length;
+	int err;
+
+	if (val <= 0 || !mode->signal_properties.pixel_clock.val ||
+	    !mode->control_properties.framerate_factor ||
+	    !mode->image_properties.line_length)
+		return -EINVAL;
+	frame_length = mode->signal_properties.pixel_clock.val *
+		(u64)mode->control_properties.framerate_factor /
+		mode->image_properties.line_length / val;
+	frame_length = clamp_t(u64, frame_length,
+		OV9281_DEFAULT_FRAME_LENGTH, OV9281_MAX_FRAME_LENGTH);
+	/* Clamp exposure before shortening VTS to avoid an overlong exposure. */
+	priv->frame_length = (u32)frame_length;
+	err = ov9281_set_exposure(tc_dev, priv->exposure_us ?: 10000);
+	if (err)
+		return err;
+	err = ov9281_write_reg(s_data, 0x380e, frame_length >> 8);
+	if (err)
+		return err;
+	return ov9281_write_reg(s_data, 0x380f, frame_length & 0xff);
+}
+
+static int ov9281_set_group_hold(struct tegracam_device *tc_dev, bool val)
+{
+	return ov9281_write_reg(tc_dev->s_data, OV9281_GROUP_HOLD, val ? 1 : 0);
+}
 
 static struct tegracam_ctrl_ops ov9281_ctrl_ops = {
 	.numctrls = ARRAY_SIZE(ctrl_cid_list),
 	.ctrl_cid_list = ctrl_cid_list,
 	.set_gain = ov9281_set_gain,
 	.set_exposure = ov9281_set_exposure,
-        .set_group_hold = imx477_set_group_hold,
+        .set_group_hold = ov9281_set_group_hold,
+	.set_frame_rate = ov9281_set_frame_rate,
 };
 
 static int ov9281_power_on(struct camera_common_data *s_data)
@@ -614,7 +612,8 @@
 	if (err)
 		return err;
 
-	return 0;
+	priv->frame_length = OV9281_DEFAULT_FRAME_LENGTH;
+	return ov9281_set_exposure(tc_dev, priv->exposure_us ?: 10000);
 }
 
 static int ov9281_start_streaming(struct tegracam_device *tc_dev)
--- a/hardware/nvidia/t23x/nv-public/overlay/tegra234-p3767-camera-p3768-ov9281-dual.dts
+++ b/hardware/nvidia/t23x/nv-public/overlay/tegra234-p3767-camera-p3768-ov9281-dual.dts
@@ -325,23 +325,23 @@
 								/*mclk_multiplier = "9.33";*/ /* 9.33 rjs */
 								pix_clk_hz = "160000000"; /* 400MHz link freq, 2 lanes, DDR, 10bpp */
 
-								gain_factor = "1";
-								framerate_factor = "1";
-								exposure_factor = "1";
+								gain_factor = "16";
+								framerate_factor = "1000000";
+								exposure_factor = "1000000";
 								min_gain_val = "16"; /* 1.00x (reg 0x3509 raw value) */
 								max_gain_val = "254"; /* 15.88x */
 								step_gain_val = "1";
 								default_gain = "16"; /* 1.00x */
 								min_hdr_ratio = "1";
 								max_hdr_ratio = "1";
-								min_framerate = "2000000"; /* 2.0 fps */
-								max_framerate = "144000000"; /* 120.0 fps */
+								min_framerate = "2000000";
+								max_framerate = "60000000";
 								step_framerate = "1";
-								default_framerate = "144000000"; /* 120.0 fps */
-								min_exp_time = "16"; /* lines; driver writes coarse time raw */
-								max_exp_time = "14160"; /* lines (OV9281_MAX_COARSE_EXPOSURE) */
+								default_framerate = "60000000";
+								min_exp_time = "10";
+								max_exp_time = "499000";
 								step_exp_time = "1";
-								default_exp_time = "1000"; /* lines, ~9.6ms */
+								default_exp_time = "10000";
 
 								embedded_metadata_height = "0";
 							};
@@ -496,23 +496,23 @@
 								/*mclk_multiplier = "9.33";*/ /* 9.33 rjs */
 								pix_clk_hz = "160000000"; /* 400MHz link freq, 2 lanes, DDR, 10bpp */
 
-								gain_factor = "1";
-								framerate_factor = "1";
-								exposure_factor = "1";
+								gain_factor = "16";
+								framerate_factor = "1000000";
+								exposure_factor = "1000000";
 								min_gain_val = "16"; /* 1.00x (reg 0x3509 raw value) */
 								max_gain_val = "254"; /* 15.88x */
 								step_gain_val = "1";
 								default_gain = "16"; /* 1.00x */
 								min_hdr_ratio = "1";
 								max_hdr_ratio = "1";
-								min_framerate = "2000000"; /* 2.0 fps */
-								max_framerate = "144000000"; /* 120.0 fps */
+								min_framerate = "2000000";
+								max_framerate = "60000000";
 								step_framerate = "1";
-								default_framerate = "144000000"; /* 120.0 fps */
-								min_exp_time = "16"; /* lines; driver writes coarse time raw */
-								max_exp_time = "14160"; /* lines (OV9281_MAX_COARSE_EXPOSURE) */
+								default_framerate = "60000000";
+								min_exp_time = "10";
+								max_exp_time = "499000";
 								step_exp_time = "1";
-								default_exp_time = "1000"; /* lines, ~9.6ms */
+								default_exp_time = "10000";
 
 								embedded_metadata_height = "0";
 							};
